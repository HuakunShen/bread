version: 2

project_name: bread

before:
  hooks:
    - go mod tidy

builds:
  - id: bread
    main: ./cmd/bread
    binary: bread
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags:
      - -s -w -X github.com/HuakunShen/bread/internal/version.Value={{ .Version }}
    mod_timestamp: "{{ .CommitTimestamp }}"
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64

archives:
  - id: archives
    ids:
      - bread
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - LICENSE
      - README.md
      - README.zh-CN.md

checksum:
  name_template: checksums.txt

snapshot:
  name_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"

# Generate a tap formula into dist. The release workflow can upload it as an
# asset and an owner-authorized tap workflow can publish it separately.
brews:
  - name: bread
    ids:
      - archives
    directory: Formula
    homepage: https://github.com/HuakunShen/bread
    description: Bounded batch file reader for coding agents
    license: MIT
    skip_upload: true
